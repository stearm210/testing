绝对路径：从最外层元素到指定元素之间所有经过元素层级的路径
1，绝对路径以/html根节点开始
如：/html/body/div/fieldset/p[1]/input
2.绝对路径对页面要求比较严格，不建议使用

相对路径：匹配任意层的元素，不限制元素的位置
1.相对路径以//开始
2.格式：//input或者//*

XPath即为XML Path的简称，这是一种再XML文档中查找元素信息的语言
#HTML可以看作是XML的一种实现，所有的Selenium用户可以
使用这种语言进行定位
XML:一种标记语言，用于数据的存储和传递，后缀.xml结尾












